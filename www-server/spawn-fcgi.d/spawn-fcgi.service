[Unit]
Description=spawn-fcgi
After=network.target

[Service]
Type=forking
EnvironmentFile=/etc/spawn-fcgi.d/spawn-fcgi
ExecStart=/usr/bin/spawn-fcgi -f ${FCGI_APPLICATION} -F ${CHILD_COUNT} -a ${IP_ADDR} -p ${PORT} -u ${USER} -g ${GROUP}
ExecReload=/bin/kill -HUP $MAINPID
ExecStop=/bin/kill -QUIT $MAINPID

[Install]
WantedBy=multi-user.target
